---
 import  Button  from "./ui/Button.astro"; // Si tienes un botón Astro, o usa <a> normal
---

<section id="home" class="relative py-20 md:py-32 overflow-hidden">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(255,97,0,0.1),transparent_70%)]"></div>
  
  <div class="container relative z-10 max-w-3xl mx-auto text-center">
    <div class="inline-block px-3 py-1 mb-6 border border-primary/30 rounded-full bg-primary/10 text-primary text-sm">
      Full-Stack Web Developer
    </div>

    <h1 class="text-4xl md:text-6xl font-bold mb-6">
      <span class="block mb-2">Hello, I'm [Your Name]</span>
      <span class="text-primary">
        <span id="typing-text">I build exceptional web experiences.</span><span id="cursor" class="animate-blink">|</span>
      </span>
    </h1>

    <p class="text-muted-foreground mb-8 text-lg">
      Crafting secure, performant, and beautiful web applications with modern technologies.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="/projects" class="bg-primary hover:bg-primary/90 text-background py-2 px-4 rounded-lg flex items-center justify-center">
        View My Work
        <svg xmlns="http://www.w3.org/2000/svg" class="ml-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </a>
      
      <!-- <a href="#contact" class="border border-primary text-primary hover:bg-primary hover:text-background py-2 px-4 rounded-lg flex items-center justify-center">
        Contact Me
      </a> -->
      <Button variant="default" size="lg" href="#contact">
        Contact Me
      </Button>
    </div>
  </div>

  <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-primary/50 to-transparent"></div>

  <script type="module" is:inline>
    const fullText = "I build exceptional web experiences.";
    const typingText = document.getElementById("typing-text");
    const cursor = document.getElementById("cursor");
    typingText.textContent = "";
    let i = 0;
    
    function typeWriter() {
      if (i < fullText.length) {
        typingText.textContent += fullText.charAt(i);
        i++;
        setTimeout(typeWriter, 100);
      }
    }

    typeWriter();

    setInterval(() => {
      cursor.classList.toggle('opacity-0');
    }, 500);
  </script>
</section>
